resources:
  pipelines:
    pipeline_dlt_order_pipln:
      name: dlt_order_pipln
      clusters:
        - label: default
          node_type_id: Standard_D4ds_v5
          num_workers: 1
      libraries:
        - notebook:
            path: ../notebook/dlt_orders_ntbk.sql
      schema: ${resources.schemas.bronze_schema.name}
      development: true
      catalog: ${resources.schemas.bronze_schema.catalog_name}
