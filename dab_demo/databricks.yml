# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "dab_demo"

include:
  - src/*.yml
  - src/env/*.yml
  - resources/jobs/*.yml
  - resources/pipelines/*.yml
  - resources/schemas/*.yml
  # - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-777862349969176.16.azuredatabricks.net/

  # The 'prod' target, used for production deployment.
  # prod:
  #   mode: production
  #   workspace:
  #     host: https://adb-777862349969176.16.azuredatabricks.net
  #     # We always use /Users/manthesh.l@simbustech.com for all resources to make sure we only have a single copy.
  #     root_path: /Users/manthesh.l@simbustech.com/.bundle/${bundle.name}/${bundle.target}
  #   run_as:
  #     # This runs as manthesh.l@simbustech.com in production. We could also use a service principal here,
  #     # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
  #     user_name: manthesh.l@simbustech.com
